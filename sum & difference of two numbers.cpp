#include <stdio.h>
int main()
{
	int a,b,sum,diff;
	printf("enter a & b values:");
	scanf("%d %d",&a,&b);
	sum=a+b;
	diff=a-b;
    printf("\nsum:%d",sum);
    printf("\ndifference:%d",diff);
    return 0;
}

